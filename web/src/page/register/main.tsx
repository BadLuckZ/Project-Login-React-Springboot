import { RegisterContainer } from "../../containers";
import { FormLayout } from "../../layout";

export const RegisterPage = () => {
  return (
    <FormLayout>
      <RegisterContainer />
    </FormLayout>
  );
};
