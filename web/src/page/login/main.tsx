import { LoginContainer } from "../../containers";
import { FormLayout } from "../../layout";

export const LoginPage = () => {
  return (
    <FormLayout>
      <LoginContainer />
    </FormLayout>
  );
};
